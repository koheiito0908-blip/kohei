# Google AI StudioでGemini APIキーを取得したら、このファイルをコピーして .env を作成し
# 下の行に実際のキーを設定してください。
#
#   cp .env.example .env
#
# .env はコミットしないでください(リポジトリの .gitignore で除外済みです)。

GEMINI_API_KEY=

# 使用するGeminiモデル名。API側の提供状況に応じて変更してください。
GEMINI_MODEL=gemini-3.8-flash
